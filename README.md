# Tab5 File Server App (`tab5-app-fileserver`)

Aplicativo isolado de servidor de arquivos HTTP/WebDAV para o **Tab5 OS**.

## Compilação e Empacotamento

```bash
chmod +x tools/build.sh
./tools/build.sh
```

O pacote `.tab5pkg` será gerado em `dist/com.tab5.fileserver.tab5pkg`.
